# Copie para ".env" (uso local) e/ou configure na Vercel.
# Precisa ser a MESMA connection string usada no projeto do painel —
# os dois leem e gravam no mesmo banco.
DATABASE_URL=

PORT=3000
NODE_ENV=development
